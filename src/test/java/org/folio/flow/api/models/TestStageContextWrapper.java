package org.folio.flow.api.models;

import org.folio.flow.api.AbstractStageContextWrapper;
import org.folio.flow.api.StageContext;

public class TestStageContextWrapper extends AbstractStageContextWrapper {

  public TestStageContextWrapper(StageContext stageContext) {
    super(stageContext);
  }
}
